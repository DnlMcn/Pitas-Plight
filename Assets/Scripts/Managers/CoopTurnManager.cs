using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoopTurnManager : MonoBehaviour
{
    public GameEvent player1TurnEnd;
    public GameEvent player2TurnEnd;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
